# Local text-to-speech dependencies
# No cloud services, no LLMs — all processing is on-device.

# Cross-platform TTS (uses macOS 'say' or Linux espeak-ng under the hood)
pyttsx3>=2.90

# Web page fetching and HTML parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0        # faster BS4 parser (optional but recommended)

# PDF text extraction
pymupdf>=1.24.0    # pip package name: pymupdf; imports as 'fitz'

# DOCX text extraction
python-docx>=1.1.0
